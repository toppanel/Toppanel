// Editable via the CMS at /admin — kept in src/content so non-technical
// staff can update contact/registration details without touching code.
export { default as COMPANY } from "@/content/company.json";
